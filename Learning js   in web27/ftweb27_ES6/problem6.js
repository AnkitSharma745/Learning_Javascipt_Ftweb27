//logical operators.
// and or not
// && || !

//input will be always boolean;

//&&--> it will be true when all the condition are true.
//the complier will check for the first false value and return it (print ), if it does not find any false value it will return the last true value.


// console.log(1 && 2 && 0);

// console.log(1 && 0 && "");

// console.log(true && 1 && -2);


// || ---> it will be true when atleast one condition is true;

// the complier will check for the first true value and return it, if it does not find any true value then it will return the last false value.


// console.log(1 || 2 || 0);

// console.log("" || null || undefined);




console.log(0 || "" && false);